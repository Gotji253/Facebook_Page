name: Scheduled football poster

on:
  schedule:
    # GitHub Actions uses UTC. These are 07:00, 10:00, 12:00, 15:00, 18:00 and 20:00 Asia/Bangkok (UTC+7).
    - cron: "0 0,3,5,8,11,13 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: facebook-page-poster
  cancel-in-progress: false

jobs:
  post:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Validate required secrets
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_TOKEN: ${{ secrets.FB_PAGE_TOKEN }}
        run: |
          test -n "$OPENAI_API_KEY" || { echo "Missing OPENAI_API_KEY secret"; exit 1; }
          test -n "$FB_PAGE_ID" || { echo "Missing FB_PAGE_ID secret"; exit 1; }
          test -n "$FB_PAGE_TOKEN" || { echo "Missing FB_PAGE_TOKEN secret"; exit 1; }

      - name: Run poster
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: gpt-5-mini
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_TOKEN: ${{ secrets.FB_PAGE_TOKEN }}
          FONT_PATH: ${{ github.workspace }}/fonts/NotoSansThai-Regular.ttf
          STATE_FILE: state.json
          OUTPUT_IMAGE: output/latest.jpg
        run: python football_poster.py

      - name: Persist state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ -f state.json ]; then
            # state.json stays ignored for local runs, but Actions must persist it.
            git add -f state.json
            if ! git diff --cached --quiet; then
              git commit -m "chore: update posted-news state"
              git push
            else
              echo "state.json is unchanged"
            fi
          else
            echo "state.json was not created; nothing to persist"
          fi
